- **Accessible** - use `label for` and `sinput` properly
- To allows multiple files to be selected, add the `multiple` attribute.
- Styled upload using the `visibility hidden`

**Browser support:**
https://caniuse.com/#search=input%20file
